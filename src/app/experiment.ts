export class Experiment {
  // assumption: string;
  // test: string;
  // metric: string;
  // criteria: string;

  constructor(
    public assumption: string,
    public test: string,
    public metric: string,
    public criteria: string
  ) { }
}
